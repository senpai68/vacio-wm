# vacio-wm
A custom, minimalist Wayland compositor (dwl)
sudo dnf install wlroots-devel libinput-devel wayland-devel \
  wayland-protocols-devel libxkbcommon-devel pixman-devel \
  fcft-devel pkg-config gcc 
 Or, depending on the distro, it might be different.
